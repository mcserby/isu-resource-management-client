import A from '../../constants/actions';
import M from '../../constants/mutations';

const state = {
};

const actions = {
};

const mutations = {
};

const getters = {};

export default {
  state,
  actions,
  getters,
  mutations,
};
