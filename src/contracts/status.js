// Status contract

class Status {
  constructor(status, key, description, crew) {
    this.status = status;
    this.key = key;
    this.description = description;
    this.crew = crew;
  }
}

export default Status;
